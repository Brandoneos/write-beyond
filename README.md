Run docker compose up --build

to run docker file and whole project.

// will run server, database, and frontend locally. 

// Database credentials are defaulted and hard coded to user: user and password: password. Change if necessary. 

